{
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "import", "tailwind", "apply", "layer", "config", "plugin", "utility", "variant"]
      }
    ]
  }
}
